<Window x:Class="MoriaMODCreator.Views.Dialogs.ImportConstructionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Construction"
        Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/Icons/app_icon.ico"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Import Construction from JSON" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>

        <!-- Source file selector -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Source JSON:" FontSize="13" VerticalAlignment="Center"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="SourcePathBox" FontSize="11" IsReadOnly="True"/>
            <Button Grid.Column="2" Content="Browse..." Padding="12,6" Margin="8,0,0,0"
                    Click="OnBrowseSource"/>
        </Grid>

        <!-- Existing constructions list -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Constructions found in file:" FontSize="13"
                           Foreground="{StaticResource TextBrush}" Margin="10,8"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5,0,5,5">
                    <ItemsControl x:Name="ConstructionsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" Margin="0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"
                                                  Margin="0,0,8,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12"
                                                   Foreground="#FFD700" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="StatusText" Text="" FontSize="12"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Padding="16,8" Click="OnCancel" Margin="0,0,8,0"/>
            <Button Content="Import Selected" Padding="16,8"
                    Background="{StaticResource BuildBrush}" Click="OnImport"/>
        </StackPanel>
    </Grid>
</Window>
