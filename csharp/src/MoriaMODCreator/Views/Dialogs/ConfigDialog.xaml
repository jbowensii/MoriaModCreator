<Window x:Class="MoriaMODCreator.Views.Dialogs.ConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

        <!-- Game Path -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Game Install Path" FontSize="13" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="GamePathBox" Grid.Column="0" FontSize="12"/>
                <Button Grid.Column="1" Content="Browse" Padding="12,6"
                        Margin="8,0,0,0" FontSize="12" Click="OnBrowseGamePath"/>
            </Grid>
        </StackPanel>

        <!-- Build Timeout -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Build Timeout (seconds)" FontSize="13" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,0,5"/>
            <TextBox x:Name="TimeoutBox" Text="120" FontSize="12" Width="100"
                     HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Debug Mode -->
        <CheckBox Grid.Row="3" x:Name="DebugCheck" Content="Enable Debug Logging"
                  FontSize="13" Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Padding="16,8" Background="{StaticResource BuildBrush}"
                    Margin="0,0,8,0" Click="OnSave"/>
            <Button Content="Cancel" Padding="16,8" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
