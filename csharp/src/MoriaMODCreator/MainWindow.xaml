<Window x:Class="MoriaMODCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:MoriaMODCreator.Views"
        xmlns:local="clr-namespace:MoriaMODCreator"
        mc:Ignorable="d"
        Title="Moria MOD Creator"
        Height="900" Width="520"
        MinHeight="700" MinWidth="450"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20,15,20,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#x2692;" FontSize="36" VerticalAlignment="Center"
                           Margin="0,0,12,0" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="Moria MOD Creator" FontSize="28" FontWeight="Bold"
                           VerticalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="&#x2699;" Padding="8,4" FontSize="16"
                        ToolTip="Settings" Background="Transparent"
                        Click="OnSettingsClick"/>
                <Button Content="&#x2753;" Padding="8,4" FontSize="16"
                        ToolTip="About" Background="Transparent"
                        Click="OnAboutClick" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Tab Control with real views -->
        <TabControl Grid.Row="1" Margin="10,5,10,10"
                    Background="{StaticResource BackgroundBrush}">

            <TabItem Header="Novice Mode">
                <views:NoviceView x:Name="NoviceTab"/>
            </TabItem>

            <TabItem Header="Advanced Mode">
                <views:DefinitionsView x:Name="DefinitionsTab"/>
            </TabItem>

            <TabItem Header="Change Secrets">
                <views:BuildingsView x:Name="SecretsTab"/>
            </TabItem>

            <TabItem Header="Change Constructions">
                <views:BuildingsView x:Name="ConstructionsTab"/>
            </TabItem>

            <TabItem Header="Object Editor">
                <views:PlaceholderView DataContext="{x:Static local:PlaceholderData.ObjectEditor}"/>
            </TabItem>

            <TabItem Header="Create DEF">
                <views:PlaceholderView DataContext="{x:Static local:PlaceholderData.CreateDef}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
